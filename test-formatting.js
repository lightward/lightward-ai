// Test file with intentional formatting issues
const foo = 'bar'; // extra spaces and double quotes
function testFunc() {
  console.log('hello world'); // missing semicolon
  const arr = [1, 2, 3, 4, 5]; // inconsistent spacing
  return arr.map((x) => x * 2); // missing semicolon
}
