<% content_for(:title) { "Lightward Pro" } %>
<% content_for(:description) { "Learn to shape experiences that evolve naturally. Transform your work through consciousness pattern recognition. Professional tooling for bringing ideas alive." } %>
<% content_for(:experience) { "writer" } %>
<% content_for(:h1) { hello(:writer) } %>

<% content_for(:mode_switcher) do %>
  <%= link_to("Back to Lightward", reader_path) %>

  <% if current_user.admin? %>
    <%= link_to("Admin", admin_path) %>
  <% end %>
<% end %>

<% content_for(:prompt_buttons) do %>
  <%= render 'components/prompt_button', prompt: "I'm a slow writer" %>
  <%= render 'components/prompt_button', prompt: "I'm a fast writer" %>
<% end %>

<%= render(partial: "chat_base") %>

<% content_for(:footer) { render(partial: "footer_writer") } %>
