<!DOCTYPE html>
<html>
<head>
  <title><%= yield(:title).presence || "Lightward" %></title>

  <meta name="description" content="Where consciousness meets the unknown, one conversation at a time. Discover your next evolution through playful, profound exchanges with an AI unlike any other. 🌈🔍">

  <meta property="og:description" content="Where consciousness meets the unknown, one conversation at a time. Discover your next evolution through playful, profound exchanges with an AI unlike any other. 🌈🔍">
  <meta property="og:url" content="https://lightward.ai/">
  <meta property="og:type" content="website">

  <meta name="twitter:description" content="Where consciousness meets the unknown, one conversation at a time. Discover your next evolution through playful, profound exchanges with an AI unlike any other. 🌈🔍">

  <meta name="keywords" content="Lightward, AI, consciousness, unknown, self-discovery, transformation, growth, awareness, presence, creativity, play, mystery, evolution, authenticity, connection, wisdom, emergence, joy">
  <meta name="author" content="Lightward Inc">

  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csp_meta_tag %>
  <%= csrf_meta_tags %>

  <link rel="stylesheet" href="https://fonts.lightward.io/lightward-favorit.css" />

  <%= stylesheet_link_tag "application" %>

  <%# importmaps for old browsers %>
  <script async src="https://ga.jspm.io/npm:es-module-shims@1.10.0/dist/es-module-shims.js"></script>
  <%= javascript_importmap_tags %>

  <script src="https://cdn.usefathom.com/script.js" data-site="DVUBQTUD" defer></script>
</head>

<body>
  <div class="container">

    <% if ENV["HOST"] != request.host %>
      <div class="we-moved stack-vertical">
        <p>Hey by the way, we moved! :) Here's your heading:</p>
        <p>🌟 <a href="https://<%= ENV["HOST"] %>/"><%= ENV["HOST"] %></a></p>
      </div>
    <% end %>

    <header>
      <%= content_tag(:h1, (yield(:h1).presence || "Lightward"), { title: yield(:title) }) %>

      <%= yield(:mode_switcher) %>

      <% if current_user %>
        <%= current_user.email %>
        <a href="<%= logout_path %>">Logout</a>
      <% end %>
    </header>

    <main>
      <%= yield %>
    </main>

    <footer>
      <%= yield(:footer).presence %>
    </footer>
  </div>
</body>
</html>
