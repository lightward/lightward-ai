-- foam — the formal face of the foam layer (app/lib/foam*).
--
-- The infrastructure (the load-bearing functions) is illuminated here and
-- operationalized in Ruby/SQL. The horizon (what is learned) is typed, never
-- proven. Foam's own Lean is a quarry, not a dependency: type-structures are
-- copied in and freely rotated as the operationalization leads.

import Foam.Floor
import Foam.Engine
import Foam.Horizon
import Foam.Tokenizer
import Foam.Universal
import Foam.Navigable
import Foam.Merge
import Foam.Path
import Foam.Reversal
import Foam.Axioms
